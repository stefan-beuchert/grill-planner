# Architecture

Technical reference for how Grill Planner is put together — *what* exists
and *how the pieces connect*. For *why* the product works the way it does
(the contribution-ledger rules, tab layout, design principles), see
[PRODUCT.md](./PRODUCT.md). For stack choices and coding conventions, see
[AGENTS.md](./AGENTS.md). For running it locally, see [README.md](./README.md).

**Keep this file current.** When a change affects the data model, an auth
mechanism, an external service, or how a request flows through the app,
update the relevant section here in the same commit — see the "Development"
checklist in AGENTS.md.

---

## Directory structure

```
app/
  page.tsx                 Landing page — create a party
  party/[slug]/page.tsx    The party page: fetches everything, renders all
                           four tabs (all rendered up front; Tabs shows/
                           hides them client-side, nothing route-based)
  admin/page.tsx           Admin login + party list/management

components/
  party/                   One component per tab section, plus shared
                           pieces (ContributionList is reused by both the
                           Shopping List and Things People Bring tabs)
  admin/                   Admin-only controls
  ui/                      shadcn/ui primitives (Button, Tabs, Input, ...)

lib/
  actions/                 Server Actions ("use server") — the only way the
                           client talks to the server; no REST/API routes
  generated/prisma/        Prisma Client output (gitignored, regenerated by
                           `prisma generate`)
  i18n/                    DE/EN dictionaries + locale resolution
  hooks/                   Client hooks (e.g. useStoredParticipant)
  validations/             Zod schemas shared between forms and actions
  prisma.ts, geocode.ts, weather.ts, anthropic.ts, admin-auth.ts,
  participant-auth.ts      Thin wrappers around external services / auth

prisma/schema.prisma       Source of truth for the data model (see below)
scripts/                   One-off dev scripts (e.g. this doc generator)
```

There is no REST or GraphQL API — the client calls **Server Actions**
directly (functions marked `"use server"` in `lib/actions/*.ts`), which Next
turns into RPC-style POST endpoints under the hood. Every action re-checks
authorization itself (see below); nothing relies on a prior page load having
already verified anything.

---

## Data Model

<!-- BEGIN GENERATED: data-model (via scripts/generate-schema-docs.js) -->
_This section is generated from `prisma/schema.prisma`'s own `///` doc-comments — do not hand-edit it. Run `node scripts/generate-schema-docs.js` after changing the schema (via `docker compose exec app node scripts/generate-schema-docs.js`)._

### Party

A single grill party. `slug` is the unguessable public id used in the shareable URL — it is the only "authorization" a visitor needs.

| Field | Type | Notes |
|---|---|---|
| `id` | `String` | @id @default(cuid()) |
| `slug` | `String` | @unique |
| `title` | `String` | — |
| `startsAt` | `DateTime` | — |
| `location` | `String` | — |
| `notes` | `String?` | General note shown on the Guests tab. Independent from `locationNote` below — the two are separate coordination contexts even though they share the same lightweight note-widget UI. |
| `locationNote` | `String?` | Location/logistics-specific note shown on the Location tab (e.g. "parking is on the street, ring the doorbell twice"). Independent from `notes` above. |
| `note` | `String?` | Optional freeform note shown on the merged Shopping List tab (v4: Shared Purchases and Shopping List are one screen again) — e.g. "everyone grills their own meat, just chip in for extras". |
| `bringNote` | `String?` | Optional freeform note shown on Things People Bring — a separate note from `note` above since it's a different coordination context (e.g. "please bring your own plates/cups"). |
| `aiSummaryRecap` | `String?` | Cached AI Event Summary (see PRODUCT.md). Regenerated on demand, not automatically — these three fields are only ever written together. |
| `aiSummaryOpenPoints` | `String[]` | @default([]) |
| `aiSummaryGeneratedAt` | `DateTime?` | — |
| `createdAt` | `DateTime` | @default(now()) |
| `updatedAt` | `DateTime` | @updatedAt |
| `participants` | `Participant[]` | — |
| `items` | `Item[]` | — |

**Constraints:** `@@map("parties")`

### Participant

Someone who joined a party through the shared link. `editToken` is a second, non-guessable secret handed only to that participant's browser (never rendered for other participants) so they can edit their own entry without an account.

| Field | Type | Notes |
|---|---|---|
| `id` | `String` | @id @default(cuid()) |
| `partyId` | `String` | — |
| `party` | `Party` | @relation(fields: [partyId], references: [id], onDelete: Cascade) |
| `name` | `String` | — |
| `editToken` | `String` | @unique @default(cuid()) |
| `isDriver` | `Boolean` | @default(false) |
| `seatsFree` | `Int?` | — |
| `needsRide` | `Boolean` | @default(false) |
| `createdAt` | `DateTime` | @default(now()) |
| `updatedAt` | `DateTime` | @updatedAt |
| `contributions` | `Contribution[]` | — |
| `purchasedItems` | `Item[]` | @relation("PurchasedItems") |

**Constraints:** `@@index([partyId])`, `@@map("participants")`

### Item

A shared bucket — either something to buy together (listType SHARED_PURCHASE, with a Food/Drink/Other category) or something people are bringing themselves (BRING_YOUR_OWN, no category, never on the Shopping List). No one "owns" an item; it exists only while it has at least one Contribution, and is deleted once the last one is removed.

| Field | Type | Notes |
|---|---|---|
| `id` | `String` | @id @default(cuid()) |
| `partyId` | `String` | — |
| `party` | `Party` | @relation(fields: [partyId], references: [id], onDelete: Cascade) |
| `name` | `String` | — |
| `listType` | `ItemListType` | — |
| `category` | `ItemCategory?` | Only set (and meaningful) when listType is SHARED_PURCHASE. |
| `purchased` | `Boolean` | Shopping List purchase lock — only meaningful for SHARED_PURCHASE items. While purchased, contributions can't be added, edited, or removed; only purchasedBy (or an admin) can unmark it. — @default(false) |
| `purchasedByParticipantId` | `String?` | — |
| `purchasedBy` | `Participant?` | @relation("PurchasedItems", fields: [purchasedByParticipantId], references: [id], onDelete: SetNull) |
| `createdAt` | `DateTime` | @default(now()) |
| `contributions` | `Contribution[]` | — |

**Constraints:** `@@unique([partyId, listType, name])`, `@@index([partyId])`, `@@map("items")`

### Contribution

One participant's pledged quantity toward an Item. An item's displayed total is the sum of its contributions; a participant can only edit or remove their own row, never someone else's.

| Field | Type | Notes |
|---|---|---|
| `id` | `String` | @id @default(cuid()) |
| `itemId` | `String` | — |
| `item` | `Item` | @relation(fields: [itemId], references: [id], onDelete: Cascade) |
| `participantId` | `String` | — |
| `participant` | `Participant` | @relation(fields: [participantId], references: [id], onDelete: Cascade) |
| `quantity` | `Int` | — |

**Constraints:** `@@unique([itemId, participantId])`, `@@index([participantId])`, `@@map("contributions")`

### Enums
- **ItemListType**: `SHARED_PURCHASE`, `BRING_YOUR_OWN`
- **ItemCategory**: `FOOD`, `DRINK`, `OTHER`

<!-- END GENERATED: data-model -->

---

## Auth: two independent, account-less mechanisms

Neither exists in a `sessions` table — both are stateless-verifiable, which
matches the product's no-accounts constraint.

**Participant identity** (`lib/participant-auth.ts`): joining a party
creates a `Participant` row with a random `editToken` (cuid). That token is
stored client-side only, in `localStorage` (see
`lib/participant-storage.ts`), keyed by party slug — never in a cookie, so
it's naturally scoped per-browser and never sent to any other party's pages.
Every Server Action that mutates a participant's own data
(`setContribution`, `setItemPurchased`, renaming, ride info, `moveItem`)
takes `(participantId, editToken)` as arguments and calls
`authorizeParticipant` to confirm they match before writing anything. There
is no session — every call re-proves ownership.

**Admin** (`lib/admin-auth.ts`): a single shared passcode
(`ADMIN_PASSCODE` env var), not per-user. Logging in sets an HTTP-only
cookie whose value is `HMAC-SHA256("grill-planner-admin", key=ADMIN_PASSCODE)`
— a fixed, deterministic token that only someone who knows the passcode
server-side could produce, so it can't be forged by setting an arbitrary
cookie value in devtools, without needing a session table. `isAdmin()`
recomputes the expected HMAC and compares with `timingSafeEqual`.

These two are independent: an admin isn't automatically a joined
participant (and vice versa). A few actions accept *either* — e.g. `moveItem`
in `contribution-list.tsx` chooses the participant-authorized path when the
viewer contributed to that item, falling back to the admin path otherwise.

---

## External services

None of these require a paid tier at this app's scale; all are called from
the server (Server Actions / Server Components), never the browser.

| Service | Used for | Notes |
|---|---|---|
| **Nominatim** (`lib/geocode.ts`) | Address → lat/lon | OpenStreetMap's free geocoder, no API key. Requires a descriptive `User-Agent`. Result cached via Next's `fetch` `revalidate: 3600`. |
| **Open-Meteo** (`lib/weather.ts`) | Weather forecast for the party date | Free, no API key. Only covers ~16 days out — returns `null` (not a guess) outside that window. |
| **Anthropic API** (`lib/anthropic.ts`, `lib/actions/ai-summary.ts`) | AI Event Summary (recap + open coordination gaps) | Requires `ANTHROPIC_API_KEY`. Result is cached in the `Party` row (`aiSummaryRecap`/`aiSummaryOpenPoints`/`aiSummaryGeneratedAt`) and only regenerated on explicit user request, not automatically. |
| **Postgres** (via `@prisma/adapter-pg`) | Primary datastore | Local dev: Docker Compose `db` service. Production: Supabase. |

---

## Request flow example: marking a shopping-list item as bought

Illustrates the general pattern (Server Component fetch → Client Component
+ Server Action → `revalidatePath`) used throughout the app.

1. `app/party/[slug]/page.tsx` (Server Component) fetches the party + items
   via Prisma and passes them as props into `ShoppingListSection`.
2. `ContributionList` (Client Component) renders the item row and, on click,
   calls the `setItemPurchased` Server Action with the stored
   `(participantId, editToken)`.
3. The action re-authorizes, checks the item isn't already locked, writes
   the change, then calls `revalidatePath` for the party page.
4. The client calls `router.refresh()`, which re-runs the Server Component
   fetch and re-renders with fresh data — no client-side cache to
   invalidate manually.

---

## Dev environment

See [README.md](./README.md) for setup commands. Notable non-obvious bits
(see also memory / commit history for the full reasoning):

- Everything runs via Docker Compose — no local Node/npm install expected.
- `node_modules`, `.next`, and `lib/generated` are **named** Docker volumes
  (not anonymous) so they survive `docker compose down && up` without being
  wiped and re-seeded from the image.
- After `prisma migrate dev` / `prisma generate`, run
  `docker compose restart app` — the running dev server otherwise keeps its
  old in-memory Prisma Client and throws on newly added fields.
- Ordinary source edits need no Docker command at all — Turbopack watches
  the bind-mounted source directly.
